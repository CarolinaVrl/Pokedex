import React from 'react';
import { useSelector } from 'react-redux';

const Pokedex = () => {
    
    return (
        <div>
            <h1>Bienvenido </h1>
            
        </div>
    );
};

export default Pokedex;